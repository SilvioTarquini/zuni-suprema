# DEPLOY
> **NUNCA rodar `railway up`.** Deploy é sempre automático via `git push origin main` (auto-deploy já ativo). O comando `railway up` sem link confirmado cria projetos novos automaticamente — foi a causa dos 29 projetos fantasmas removidos em 09/07/2026.

Projeto Railway oficial: `zuni-suprema`

ID do projeto: `8bbefc4d-48a7-4535-8dc4-d6b24ab4e894`

URL de produção correta: `https://zuni-suprema-production.up.railway.app`

> **AVISO IMPORTANTE:** ANTES de qualquer deploy, SEMPRE rodar `railway status` e confirmar que o projeto vinculado é exatamente este. Se aparecer qualquer outro nome, PARAR e rodar `railway link` para corrigir antes de continuar.

Data de criação desta trava: `2026-07-08`
